# Public — exposed in browser bundle. Safe to share.
NEXT_PUBLIC_SUPABASE_URL=https://YOUR-PROJECT.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=eyJhbGc...

# Public — used to build callback URLs.
NEXT_PUBLIC_SITE_URL=https://ai-ofm-eta.vercel.app

# 🚨 Secret — server-only, never expose. Used by /api/invite to bypass RLS.
SUPABASE_SERVICE_ROLE_KEY=eyJhbGc...
